# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

This is a new project. Update this file as the codebase develops with build commands, architecture notes, and conventions.

**Note:** This project uses `bun` as its package manager. Use `bun run dev` and `bun run build` instead of `npm`.
